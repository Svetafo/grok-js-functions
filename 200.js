// 200. Объявите функцию foo. Внутри неё объявите функцию bar. Обозначьте границы тела
// функции foo. Обозначьте границы тела функции bar.

function foo() {/*body foo start*/
    function bar() { /*body bar start*/

    /*body bar end*/}
    /*body foo end*/}
