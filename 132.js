// 132. Объявите функцию foo, которая суммирует первые два значения, поданные ей на вход.
// Вызовите foo со входящими значениями 1, 2, 3. Сколько у функции параметров?
// А сколько аргументов? Каков результат выполнения функции?

function foo(a, b/*2 params*/) {
    return a+b
}

foo(1, 2, 3/*3 arguments*/);

/*result === 3*/
