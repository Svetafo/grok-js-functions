// 202. Объявите функцию foo. Внутри неё объявите функцию bar. Внутри bar объявите функцию baz.
// Обозначьте границы тела функции foo. Обозначьте границы тела функции bar.
// Обозначьте границы тела функции baz.

function foo() {/*body foo start*/
    function bar() {/*body bar start*/
        function baz() {/*body baz start*/

            /*body baz end*/}

        /*body bar end*/}

    /*body foo end*/}
