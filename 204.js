// 204. Объявите функцию foo со входящим параметром a. Внутри неё объявите функцию bar
// со входящим параметром b. Обозначьте границы параметров функции foo.
// Обозначьте границы параметров функции bar.

function foo(/*param foo start*/a/*param foo end*/) {
    function bar(/*param bar start*/b/*param bar end*/) {

    }

}
